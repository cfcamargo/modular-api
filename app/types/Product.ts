export type ProductProps = {
    name: string,
    quantity: number,
    brand: string
    price: number
}

export type UpdateProductProps = {
    id: number
    name: string,
    quantity: number,
    brand: string
    price: number
}